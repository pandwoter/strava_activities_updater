# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

gem 'dotenv'
gem 'strava-ruby-client', '~> 0.4.3'

group :development do
  gem 'pry'
  gem 'pry-nav'
  gem 'rubocop'
end
